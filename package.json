{
  "name": "desafio-connecta",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter \"./packages/backend\" --filter \"./packages/frontend\" --parallel run dev",
    "build": "pnpm --filter \"./packages/frontend...\" run build"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}